package com.highwire.interview;

import org.junit.Assert;
import org.junit.Test;

/**
 * Example test that proves they run as part of the maven build.
 * You can add your own test class by putting in src/test/java and making sure the class name ends in "Test".
 */
public class ExampleUnitTest {

    @Test
    public void exampleTest() {
        Assert.assertTrue(true);
    }
}
